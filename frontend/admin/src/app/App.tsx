// Корневой компонент админки. Этап 0 — пустой скелет.
// Экраны (вход, список страниц, CRUD компонентов, смена статуса) — этап 1.
export function App() {
  return (
    <main>
      <h1>HealthPage Admin</h1>
      <p>Каркас админки (этап 0). Бизнес-экраны появятся на этапе 1.</p>
    </main>
  );
}
