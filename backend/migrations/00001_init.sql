-- Начальная миграция-заглушка (этап 0).
-- Реальная схема (Account, User, StatusPage, Component, ...) появится на этапе 1.
-- Здесь только подтверждаем, что цепочка миграций goose работает на чистой БД.

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
